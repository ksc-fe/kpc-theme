import {Split} from 'kpc';
import Code from '../code';

<Split class="code-preview">
    <b:first>
        <Code code={{ self.get('variables') }} />
    </b:first>
    <b:last>
        <Code code={{ self.get('code') }} />
    </b:last>
</Split>
